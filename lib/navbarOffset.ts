/**
 * Matches fixed header in `components/layout/Navbar.tsx`:
 * `nav` py-3 (top + bottom) + logo h-9 + header border-b (1px).
 * Use as padding-top / scroll-margin on content below the fixed navbar.
 */
export const NAVBAR_HEIGHT_OFFSET_CLASS =
  "pt-[calc(0.75rem+2.25rem+0.75rem+1px)]";
